package com.studymate.domain.onboarding.domain.repository;

import com.studymate.domain.onboarding.entity.GroupSize;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GroupSizeRepository extends JpaRepository<GroupSize,Integer> {
}
