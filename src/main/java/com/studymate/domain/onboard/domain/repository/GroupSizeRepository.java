package com.studymate.domain.onboard.domain.repository;

import com.studymate.domain.onboard.entity.GroupSize;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GroupSizeRepository extends JpaRepository<GroupSize,Integer> {
}
