# 환경별 설정 예시 파일
# 실제 사용시 .env.dev, .env.prod 등으로 복사하여 사용

# ===========================================
# 데이터베이스 설정
# ===========================================
# MySQL 연결 정보
DB_HOST=localhost
DB_PORT=3306
DB_NAME=studymate
DB_USER=studymate
DB_PASSWORD=studymate123

# Redis 연결 정보  
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis123

# ===========================================
# 애플리케이션 설정
# ===========================================
# JWT 시크릿 키 (운영환경에서는 강력한 키로 변경)
JWT_SECRET_KEY=dev-jwt-secret-key-change-in-production

# 네이버 API 설정
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret

# ===========================================
# 컨테이너 레지스트리 설정 (운영환경)
# ===========================================
DOCKER_REGISTRY=your-registry.kr1.ncr.ntruss.com
APP_IMAGE_NAME=studymate-app
APP_IMAGE_VERSION=latest

# ===========================================
# NCP 설정 (운영환경)
# ===========================================
# NCP Access Key (CI/CD용)
NCP_ACCESS_KEY=your_ncp_access_key
NCP_SECRET_KEY=your_ncp_secret_key

# 서버 접속 정보 (배포용)
SERVER_HOST=your_server_private_ip
SERVER_USER=ubuntu
# SERVER_SSH_KEY는 GitHub Secrets에 저장

# ===========================================
# 기타 설정
# ===========================================
TZ=Asia/Seoul
SPRING_PROFILES_ACTIVE=dev