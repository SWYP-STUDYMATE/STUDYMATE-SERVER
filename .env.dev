# 로컬 개발 환경 설정
SPRING_PROFILES_ACTIVE=dev

# 데이터베이스 설정 (Docker Compose에서 실행)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=studymate
DB_USER=studymate
DB_PASSWORD=studymate123

# Redis 설정 (Docker Compose에서 실행)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis123

# JWT 설정 (개발용)
JWT_SECRET_KEY=dev-jwt-secret-key-for-local-testing-only

# 네이버 API 설정 (테스트용 더미 값)
NAVER_CLIENT_ID=test_client_id
NAVER_CLIENT_SECRET=test_client_secret

# 컨테이너 설정
APP_CONTAINER_NAME=studymate-app-dev
DB_CONTAINER_NAME=studymate-mysql-dev
REDIS_CONTAINER_NAME=studymate-redis-dev
DB_ROOT_PASSWORD=root123

# 기타
TZ=Asia/Seoul